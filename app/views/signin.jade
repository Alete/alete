extends ./layouts/main

block content
    .container.authForm
        form(action="/signin", method="post").styled
            div
                label Email:
                input(type="text", name="email")
            div
                label Password:
                input(type="password", name="password")
            div
                input(type="submit", value="Signin").button
